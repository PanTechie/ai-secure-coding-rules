---
name: PHP Security
description: >
  Activate when writing or reviewing PHP code involving SQL queries, HTML output,
  file uploads, XML parsing, unserialize/deserialization, shell execution, session
  management, cryptography, file inclusion, CSRF protection, mail(), stream wrappers,
  extract(), assert(), switch comparisons, or preg_replace.
  Also activate when the user mentions CVE, injection, type juggling, POP chain,
  LFI, RFI, or asks for a PHP security review.
---

## Use this skill when

Activate when writing or reviewing PHP code involving SQL queries, HTML output, file uploads, XML parsing, unserialize/deserialization, shell execution, session management, cryptography, file inclusion, CSRF protection, mail(), stream wrappers, extract(), assert(), switch comparisons, or preg_replace. Also activate when the user mentions CVE, injection, type juggling, POP chain, LFI, RFI, or asks for a PHP security review.

## Instructions

Read the `rules.md` file in this skill directory and apply those security rules to all code analysis and generation tasks.
