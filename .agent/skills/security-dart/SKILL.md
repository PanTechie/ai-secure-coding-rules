---
name: Dart / Flutter Security
description: >
  Activate when writing or reviewing Dart or Flutter code involving Random()/Random.secure(),
  SharedPreferences/Hive/flutter_secure_storage, HttpClient/badCertificateCallback/dio TLS,
  sqflite SQL queries, Process.run()/Process.start() command execution, dart:io File operations,
  dart:ffi/Pointer/Arena memory management, WebView/JavascriptChannel, deep links/URL schemes,
  MethodChannel platform channels, dart:mirrors reflection, jsonDecode deserialization,
  JWT validation, package:encrypt/package:cryptography/package:pointycastle AES/cipher usage,
  print()/debugPrint() logging, --dart-define secrets, kDebugMode/kReleaseMode,
  dart pub audit/pubspec.yaml/pubspec.lock dependency management, or biometric authentication.
  Also activate when the user mentions CVE, flutter_downloader, dio CRLF, Serverpod, blutter,
  OWASP MASVS, MobSF, frida hooking, certificate pinning, or asks for a Dart or Flutter security review.
---

## Use this skill when

Activate when writing or reviewing Dart or Flutter code involving Random()/Random.secure(),
SharedPreferences/Hive/flutter_secure_storage, HttpClient/badCertificateCallback/dio TLS,
sqflite SQL queries, Process.run()/Process.start() command execution, dart:io File operations,
dart:ffi/Pointer/Arena memory management, WebView/JavascriptChannel, deep links/URL schemes,
MethodChannel platform channels, dart:mirrors reflection, jsonDecode deserialization,
JWT validation, package:encrypt/package:cryptography/package:pointycastle AES/cipher usage,
print()/debugPrint() logging, --dart-define secrets, kDebugMode/kReleaseMode,
dart pub audit/pubspec.yaml/pubspec.lock dependency management, or biometric authentication.
Also activate when the user mentions CVE, flutter_downloader, dio CRLF, Serverpod, blutter,
OWASP MASVS, MobSF, frida hooking, certificate pinning, or asks for a Dart or Flutter security review.

## Instructions

Read the `rules.md` file in this skill directory and apply those security rules to all code analysis and generation tasks.
