---
name: JavaScript & TypeScript Security
description: >
  Activate when writing or reviewing code involving eval/new Function, innerHTML/DOM sinks,
  child_process/exec, prototype pollution, Object.assign/merge with user input, path.join,
  JSON.parse/deserialize, MongoDB queries, Math.random, jwt.verify, fetch with user URLs,
  regex on user input, npm dependencies, or TypeScript type assertions (as any, @ts-ignore).
  Also activate when the user mentions CVE, prototype pollution, ReDoS, SSRF, XSS, NoSQL
  injection, supply chain, or asks for a JavaScript/TypeScript/Node.js security review.
---

## Use this skill when

Activate when writing or reviewing code involving eval/new Function, innerHTML/DOM sinks, child_process/exec, prototype pollution, Object.assign/merge with user input, path.join, JSON.parse/deserialize, MongoDB queries, Math.random, jwt.verify, fetch with user URLs, regex on user input, npm dependencies, or TypeScript type assertions (as any, @ts-ignore). Also activate when the user mentions CVE, prototype pollution, ReDoS, SSRF, XSS, NoSQL injection, supply chain, or asks for a JavaScript/TypeScript/Node.js security review.

## Instructions

Read the `rules.md` file in this skill directory and apply those security rules to all code analysis and generation tasks.
